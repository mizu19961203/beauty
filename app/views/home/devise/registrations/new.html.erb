<div class="signup-page">
  <div class="container signup-page-container">
    <h1 class="heading">新規登録</h1>
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <div class="form-box">
        <div class="form-item">
          <%= f.label :email, "Eメールアドレス" %><br />
          <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
        </div>
        <div class="form-item">
          <%= f.label :password, "パスワード(6文字以上)" %>
          <%= f.password_field :password, autocomplete: "new-password" %>
        </div>
        <div class="form-item">
          <%= f.label :password_confirmation %><br />
          <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
        </div>
        <div class="remember">
          <% if devise_mapping.rememberable? %>
          <%= f.check_box :remember_me %>
          <%= f.label :remember, "ログインしたままにする" %>
          <% end %>
        </div>
        <div class="action submit-action">
          <%= f.submit "新規登録" %>
        </div>
        <div class="caution">
          続行することで、Beautyの
          <%= link_to "利用規約", "#" %>
          および
          <%= link_to "プライバシー規約", "#" %>
          に同意するものとみなされます。
        </div>
        <div class="text-link">
          <p>すでにアカウントをお持ちの方は<%= link_to "こちら", new_user_session_path %>からログインしてください</p>
        </div>
    <% end %>
  </div>
</div>
